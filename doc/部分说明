创建帐号
    ../src/api/AccountService.ts 文件
    1 使用 AccountService.createAccount()
    2 使用 Account.generateNewAccount(netType);
    3 通过私钥创建帐号 Account.createFromPrivateKey(private,netType)
数据上链
    ../src/api/HttpService.ts 文件

    参数说明：
        systemAccountPrivateKey 不用角色对应系统帐号私钥
        tab 自定义标签
        recipientPublicKey 等待认证帐号的公钥
        data 认证数据 最好为jsonStr  例如: const data = IdentityModel.toJsonStr(identityData);
        isEncrypt 是否加密

    方法：为通用方法，任意数据都可以通过改方法上链
        HttpService.uptoChainServiceCommon（systemAccountPrivateKey,tab,recipientPublicKey,data,isEncrypt）

配置：
    ../src/config/config.ts文件
    里面包含：服务器节点，节点服务器generationHash ，系统帐号等

系统帐号说明：

    不同的系统帐号有着不同的功能，比如身份上链，系统帐号则需要传递identityAccount帐号例如：
    publicKey 待认证帐号的公钥
    HttpService.uptoChainServiceCommon(identityAccount.private, TabType.identity, publicKey, data, false);

如何获取公钥

    通过私钥获取
    const account = Account.createFromPrivateKey(private,netType)
    account.publicKey

如果根据公钥获取地址
    const publicAccount = PublicAccount.createFromPublicKey(publicKey, netType);
    publicAccount包含地址对象和公钥

获取地址字符串 address.plain(): string;




